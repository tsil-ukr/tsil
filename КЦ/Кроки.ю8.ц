взяти визначення ./КЦ;

секція КЦ {
  зовнішня дія знищити_кроки(компілятор: адреса<Компілятор>, кроки: Кроки) {
    змінна п: позитивне = 0;

    поки п < кроки.розмір {
      знищити_крок(компілятор, кроки.дані[п]);

      п += 1;
    }

    звільнити_памʼять(компілятор, кроки.дані);
  }
}