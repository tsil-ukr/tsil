взяти означення ./КЦ;

простір КЦ {
  дія знищити_кроки(компілятор: адреса<Компілятор>, кроки: Кроки) {
    змінна п: природне = 0;

    поки п < кроки.розмір {
      знищити_крок(компілятор, кроки.дані[п]);

      п += 1;
    }

    біб::звільнити_памʼять(кроки.дані);
  }
}