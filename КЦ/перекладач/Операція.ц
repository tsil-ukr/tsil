взяти визначення КЦ;

простір КЦ {
  дія скомпілювати_операцію(
    компілятор: *Компілятор,
    контекст: Контекст,
    дані_операції: розбирач::ДаніСполукиОперація,
    місцезнаходження: розбирач::Місцезнаходження,
    вихід: *Обʼєкт,
    вихід_помилки: **Помилка
  ) -> логічне {
    змінна успіх = ні;
    змінна помилка: *Помилка;

    змінна ліво: Обʼєкт;
    змінна обʼєкт_право: Обʼєкт;

    успіх = скомпілювати_значення(
      компілятор,
      контекст,
      пусто,
      дані_операції.ліво,
      пусто,
      ліво::адреса,
      вихід_помилки
    );
    якщо не успіх {
      вернути ні;
    }

    змінна очікуваний_тип_право: *ОбʼєктТипу = пусто;

    якщо ліво.вид == ВидОбʼєктаЗначення {
      змінна дані_обʼєкта = ліво.дані як ОбʼєктЗначення;
      очікуваний_тип_право = дані_обʼєкта.тип;
    } інакше якщо ліво.вид == ВидОбʼєктаНезавантаженеЗначення {
      змінна дані_обʼєкта = ліво.дані як ОбʼєктНезавантаженогоЗначення;
      очікуваний_тип_право = дані_обʼєкта.тип;
    }

    успіх = скомпілювати_значення(
      компілятор,
      контекст,
      пусто,
      дані_операції.право,
      очікуваний_тип_право,
      обʼєкт_право::адреса,
      вихід_помилки
    );
    якщо не успіх {
      вернути ні;
    }

    якщо дані_операції.вид == розбирач::ВидОпераціїДодавання {
      вернути обʼєкт_додати(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїВіднімання {
      успіх = скомпілювати_значення(
        компілятор,
        контекст,
        пусто,
        дані_операції.право,
        очікуваний_тип_право,
        обʼєкт_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      вернути обʼєкт_відняти(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїМноження {
      вернути обʼєкт_помножити(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїДілення {
      вернути обʼєкт_поділити(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїМодуль {
      вернути обʼєкт_поділити_за_модулем(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїЗсувВліво {
      вернути обʼєкт_зсунути_вліво(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїЗсувВправо {
      вернути обʼєкт_зсунути_вправо(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїБільше {
      вернути обʼєкт_більше(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїМенше {
      вернути обʼєкт_менше(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїБільшеРівне {
      вернути обʼєкт_більше_рівно(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїМеншеРівне {
      вернути обʼєкт_менше_рівно(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїРівне {
      вернути обʼєкт_рівно(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїНерівне {
      вернути обʼєкт_не_рівно(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїДІ {
      вернути обʼєкт_ді(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїДАБО {
      вернути обʼєкт_дабо(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїВАБО {
      вернути обʼєкт_вабо(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїІ {
      змінна обʼєкт_значення: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        ліво,
        компілятор.тип_логічне,
        обʼєкт_значення::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }

      змінна пм_значення_результату = ПМ::вказівка_виділити(
        контекст.кк_дії.крок_накопичень.пм_крок,
        компілятор.тип_логічне.пм_тип
      );

      змінна крок_інакше = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_інакше"));
      змінна крок_успіху = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_успіх"));
      змінна крок_невдачі = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_невдача"));
      змінна крок_виходу = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_вихід"));

      ПМ::вказівка_записати(
        крок_успіху,
        компілятор.обʼєкт_значення_так.пм_значення,
        пм_значення_результату
      );
      ПМ::вказівка_стрибнути(
        крок_успіху,
        крок_виходу
      );

      ПМ::вказівка_записати(
        крок_невдачі,
        компілятор.обʼєкт_значення_ні.пм_значення,
        пм_значення_результату
      );
      ПМ::вказівка_стрибнути(
        крок_невдачі,
        крок_виходу
      );

      змінна аргумент1_нуль: Аргумент;
      якщо обʼєкт_значення.тип == компілятор.тип_ц8 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц8, ПМ::створити_ц8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц16 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц16, ПМ::створити_ц16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц32, ПМ::створити_ц32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц64, ПМ::створити_ц64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_логічне {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_логічне, ПМ::створити_логічне(компілятор.пм_модуль, ні) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п8 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п8, ПМ::створити_п8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п16 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п16, ПМ::створити_п16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п32, ПМ::створити_п32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п64, ПМ::створити_п64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_р32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р32, ПМ::створити_р32(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_р64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р64, ПМ::створити_р64(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }

      змінна обʼєкт_рівно_ліво: Обʼєкт;

      успіх = обʼєкт_значення_рівно(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_значення,
        аргумент1_нуль,
        обʼєкт_рівно_ліво::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_рівно_ліво: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_рівно_ліво,
        компілятор.тип_логічне,
        обʼєкт_значення_рівно_ліво::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      якщо обʼєкт_значення_рівно_ліво.тип != компілятор.тип_логічне {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }
      ПМ::вказівка_стрибнути_якщо(
        контекст.кк_дії.поточний_крок.пм_крок,
        обʼєкт_значення_рівно_ліво.пм_значення,
        крок_невдачі,
        крок_інакше
      );


      контекст.кк_дії.поточний_крок = Крок {
        отримати_назву(компілятор, "або_інакше"),
        контекст.кк_дії.обʼєкт_дії,
        крок_інакше
      };

      успіх = скомпілювати_значення(
        компілятор,
        контекст,
        пусто,
        дані_операції.право,
        очікуваний_тип_право,
        обʼєкт_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_право: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        дані_операції.право.місцезнаходження,
        обʼєкт_право,
        пусто,
        обʼєкт_значення_право::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      змінна аргумент2_нуль: Аргумент;
      якщо обʼєкт_значення_право.тип == компілятор.тип_ц8 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц8, ПМ::створити_ц8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц16 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц16, ПМ::створити_ц16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц32, ПМ::створити_ц32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц64, ПМ::створити_ц64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_логічне {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_логічне, ПМ::створити_логічне(компілятор.пм_модуль, ні) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п8 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п8, ПМ::створити_п8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п16 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п16, ПМ::створити_п16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п32, ПМ::створити_п32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п64, ПМ::створити_п64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_р32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р32, ПМ::створити_р32(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_р64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р64, ПМ::створити_р64(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }

      змінна обʼєкт_рівно_право: Обʼєкт;

      успіх = обʼєкт_значення_рівно(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_значення_право,
        аргумент2_нуль,
        обʼєкт_рівно_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_рівно_право: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_рівно_право,
        компілятор.тип_логічне,
        обʼєкт_значення_рівно_право::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      якщо обʼєкт_значення_рівно_право.тип != компілятор.тип_логічне {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }
      ПМ::вказівка_стрибнути_якщо(
        контекст.кк_дії.поточний_крок.пм_крок,
        обʼєкт_значення_рівно_право.пм_значення,
        крок_невдачі,
        крок_успіху
      );

      контекст.кк_дії.поточний_крок = Крок {
        отримати_назву(компілятор, "або_вихід"),
        контекст.кк_дії.обʼєкт_дії,
        крок_виходу
      };

      вихід::вміст = Обʼєкт {
        ВидОбʼєктаНезавантаженеЗначення,
        ОбʼєктНезавантаженогоЗначення { компілятор.тип_логічне, пм_значення_результату }
      };
      вернути так;
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїАБО {
      змінна обʼєкт_значення: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        ліво,
        компілятор.тип_логічне,
        обʼєкт_значення::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }

      змінна пм_значення_результату = ПМ::вказівка_виділити(
        контекст.кк_дії.крок_накопичень.пм_крок,
        компілятор.тип_логічне.пм_тип
      );

      змінна крок_інакше = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_інакше"));
      змінна крок_успіху = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_успіх"));
      змінна крок_невдачі = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_невдача"));
      змінна крок_виходу = ПМ::створити_крок(контекст.кк_дії.обʼєкт_дії.пм_інструкція, отримати_назву(компілятор, "або_вихід"));

      ПМ::вказівка_записати(
        крок_успіху,
        компілятор.обʼєкт_значення_так.пм_значення,
        пм_значення_результату
      );
      ПМ::вказівка_стрибнути(
        крок_успіху,
        крок_виходу
      );

      ПМ::вказівка_записати(
        крок_невдачі,
        компілятор.обʼєкт_значення_ні.пм_значення,
        пм_значення_результату
      );
      ПМ::вказівка_стрибнути(
        крок_невдачі,
        крок_виходу
      );

      змінна аргумент1_нуль: Аргумент;
      якщо обʼєкт_значення.тип == компілятор.тип_ц8 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц8, ПМ::створити_ц8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц16 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц16, ПМ::створити_ц16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц32, ПМ::створити_ц32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_ц64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц64, ПМ::створити_ц64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_логічне {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_логічне, ПМ::створити_логічне(компілятор.пм_модуль, ні) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п8 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п8, ПМ::створити_п8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п16 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п16, ПМ::створити_п16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п32, ПМ::створити_п32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_п64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п64, ПМ::створити_п64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_р32 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р32, ПМ::створити_р32(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення.тип == компілятор.тип_р64 {
        аргумент1_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р64, ПМ::створити_р64(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }

      змінна обʼєкт_не_рівно_ліво: Обʼєкт;

      успіх = обʼєкт_значення_не_рівно(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_значення,
        аргумент1_нуль,
        обʼєкт_не_рівно_ліво::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_не_рівно_ліво: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_не_рівно_ліво,
        компілятор.тип_логічне,
        обʼєкт_значення_не_рівно_ліво::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      якщо обʼєкт_значення_не_рівно_ліво.тип != компілятор.тип_логічне {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }
      ПМ::вказівка_стрибнути_якщо(
        контекст.кк_дії.поточний_крок.пм_крок,
        обʼєкт_значення_не_рівно_ліво.пм_значення,
        крок_успіху,
        крок_інакше
      );


      контекст.кк_дії.поточний_крок = Крок {
        отримати_назву(компілятор, "або_інакше"),
        контекст.кк_дії.обʼєкт_дії,
        крок_інакше
      };
      успіх = скомпілювати_значення(
        компілятор,
        контекст,
        пусто,
        дані_операції.право,
        очікуваний_тип_право,
        обʼєкт_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_право: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        дані_операції.право.місцезнаходження,
        обʼєкт_право,
        пусто,
        обʼєкт_значення_право::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      змінна аргумент2_нуль: Аргумент;
      якщо обʼєкт_значення_право.тип == компілятор.тип_ц8 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц8, ПМ::створити_ц8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц16 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц16, ПМ::створити_ц16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц32, ПМ::створити_ц32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_ц64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_ц64, ПМ::створити_ц64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_логічне {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_логічне, ПМ::створити_логічне(компілятор.пм_модуль, ні) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п8 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п8, ПМ::створити_п8(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п16 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п16, ПМ::створити_п16(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п32, ПМ::створити_п32(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_п64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_п64, ПМ::створити_п64(компілятор.пм_модуль, 0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_р32 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р32, ПМ::створити_р32(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше якщо обʼєкт_значення_право.тип == компілятор.тип_р64 {
        аргумент2_нуль = Аргумент { Обʼєкт { ВидОбʼєктаЗначення, дані = ОбʼєктЗначення { компілятор.тип_р64, ПМ::створити_р64(компілятор.пм_модуль, 0.0) } }, місцезнаходження };
      } інакше {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }

      змінна обʼєкт_не_рівно_право: Обʼєкт;

      успіх = обʼєкт_значення_не_рівно(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_значення_право,
        аргумент2_нуль,
        обʼєкт_не_рівно_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      змінна обʼєкт_значення_не_рівно_право: ОбʼєктЗначення;

      успіх = отримати_як_значення(
        компілятор,
        контекст,
        місцезнаходження,
        обʼєкт_не_рівно_право,
        компілятор.тип_логічне,
        обʼєкт_значення_не_рівно_право::адреса,
        помилка::адреса
      );
      якщо не успіх {
        вихід_помилки::вміст = помилка;
        вернути ні;
      }
      якщо обʼєкт_значення_не_рівно_право.тип != компілятор.тип_логічне {
        вихід_помилки::вміст = створити_помилку(
          компілятор,
          "Не вдалось скомпілювати",
          створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
        );
        вернути ні;
      }
      ПМ::вказівка_стрибнути_якщо(
        контекст.кк_дії.поточний_крок.пм_крок,
        обʼєкт_значення_не_рівно_право.пм_значення,
        крок_успіху,
        крок_невдачі
      );

      контекст.кк_дії.поточний_крок = Крок {
        отримати_назву(компілятор, "або_вихід"),
        контекст.кк_дії.обʼєкт_дії,
        крок_виходу
      };

      вихід::вміст = Обʼєкт {
        ВидОбʼєктаНезавантаженеЗначення,
        ОбʼєктНезавантаженогоЗначення { компілятор.тип_логічне, пм_значення_результату }
      };
      вернути так;
    } інакше якщо дані_операції.вид == розбирач::ВидОпераціїЯк {
      успіх = скомпілювати_значення(
        компілятор,
        контекст,
        пусто,
        дані_операції.право,
        очікуваний_тип_право,
        обʼєкт_право::адреса,
        вихід_помилки
      );
      якщо не успіх {
        вернути ні;
      }

      вернути обʼєкт_як(компілятор, контекст, місцезнаходження, ліво, Аргумент { обʼєкт_право, дані_операції.право.місцезнаходження }, вихід, вихід_помилки);
    }

    вихід_помилки::вміст = створити_помилку(
      компілятор,
      "Операція не підтримується.",
      створити_місцезнаходження_помилки_з_місцезнаходження_розбирача(компілятор, контекст.розібраний_текст, місцезнаходження)
    );
    вернути ні;
  }
}