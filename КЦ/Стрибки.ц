взяти визначення ./КЦ;

простір КЦ {
  дія знищити_стрибки(компілятор: *Компілятор, стрибки: Стрибки) {
    змінна п: природне = 0;

    поки п < стрибки.розмір {
      знищити_стрибок(компілятор, стрибки.дані[п]);

      п += 1;
    }

    біб::звільнити(стрибки.дані);
  }
}