взяти визначення ./КЦ;

простір КЦ {
  місцева дія знищити_стрибки(компілятор: адреса<Компілятор>, стрибки: Стрибки) {
    змінна п: природне = 0;

    поки п < стрибки.розмір {
      знищити_стрибок(компілятор, стрибки.дані[п]);

      п += 1;
    }

    біб::звільнити_памʼять(стрибки.дані);
  }
}