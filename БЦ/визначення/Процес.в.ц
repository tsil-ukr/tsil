взяти визначення ./Шлях;

дія __біб__отримати_робочу_директорію_процесу(вихід: адреса<біб::Шлях>) -> ціле;
дія __біб__виконати_системну_команду(розмір: природне, дані: адреса<п8>) -> ціле;

простір біб {
  // щоразу виділяє нове значення, яке потрібно звільнити вручну
  внутрішня дія отримати_робочу_директорію_процесу(вихід: адреса<Шлях>) -> ціле {
    вернути __біб__отримати_робочу_директорію_процесу(вихід);
  }

  внутрішня дія виконати_системну_команду(розмір: природне, дані: адреса<п8>) -> ціле {
    вернути __біб__виконати_системну_команду(розмір, дані);
  }
}