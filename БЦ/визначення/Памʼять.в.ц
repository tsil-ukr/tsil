дія __біб__виділити_сиру_памʼять(розмір: природне) -> *п8;
дія __біб__перевиділити_сиру_памʼять(значення: *п8, розмір: природне) -> *п8;
дія __біб__звільнити_сиру_памʼять(значення: *п8);

простір біб {
  внутрішня дія виділити<Т>(кількість: природне) -> *Т;
  внутрішня дія перевиділити<Т>(значення: *Т, кількість: природне) -> *Т;
  внутрішня дія звільнити(значення: адреса);

  внутрішня дія виділити<Т>(кількість: природне) -> *Т {
    вернути __біб__виділити_сиру_памʼять(кількість * Т.розмір) як *Т;
  }

  внутрішня дія перевиділити<Т>(значення: *Т, кількість: природне) -> *Т {
    вернути __біб__перевиділити_сиру_памʼять(значення як *п8, кількість * Т.розмір) як *Т;
  }

  внутрішня дія звільнити(значення: адреса) {
    __біб__звільнити_сиру_памʼять(значення як *п8);
  }
}